{
  "name": "acc-telemetry-server",
  "version": "1.0.0",
  "description": "WebSocket relay + dashboard host for ACC telemetry (Render)",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
